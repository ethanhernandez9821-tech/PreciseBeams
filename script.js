const orb=document.querySelector('.cursor-orb');if(orb&&matchMedia('(pointer: fine)').matches){let tx=innerWidth/2,ty=innerHeight/2,x=tx,y=ty;addEventListener('mousemove',e=>{tx=e.clientX;ty=e.clientY;orb.classList.add('visible')});const draw=()=>{x+=(tx-x)*.16;y+=(ty-y)*.16;orb.style.transform=`translate3d(${x}px,${y}px,0)`;requestAnimationFrame(draw)};requestAnimationFrame(draw)}